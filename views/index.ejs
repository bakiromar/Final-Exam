<html>
  <head>
    
    <title>Weather Page</title>
   
   
  </head>
  <body>
    <p>Please Enter a city in the Search box to get the weather info</p>
    <div class="container">
      <fieldset>
        <form action="/weather" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Enter a City" required>
          <input type="submit" class="ghost-button" value="Weather">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
    <p>The Temperature is showing in Fahrenheit!</p>

  </body>
</html>